[project]
name = "sa-instantly"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "upstash-redis==1.7.0",
    "requests==2.32.3",
    "dnspython==2.8.0",
]

[tool.vercel]
entrypoint = "api.index:handler"
